import { h } from 'preact';
import { TextField, Button } from '../../common';

// TODO: construct common Landing route!
export default () => (
  <div>
    <Button />
    <TextField />
    <p>Landing</p>
  </div>
)
